## 🎉 项目启动成功！

### ✅ 运行状态

**后端服务**
- 地址: http://localhost:8000
- API 文档: http://localhost:8000/docs
- 状态: ✅ 运行中

**前端服务**
- 地址: http://localhost:3000
- 状态: ✅ 运行中

**数据库**
- 测试用户已创建
- 业务数据已填充（596 个订单，49 条库存，150 条财务流水）

---

### 🔐 登录信息

**管理员账号**
- 用户名: `admin`
- 密码: `admin123`

**普通用户账号**
- 用户名: `user`
- 密码: `user123`

---

### 🚀 快速开始

#### 1. 访问前端
打开浏览器访问: http://localhost:3000

#### 2. 获取 Token (两种方式)

**方式一: 使用 API 文档**
1. 访问 http://localhost:8000/docs
2. 找到 `POST /api/v1/auth/login`
3. 点击 "Try it out"
4. 输入用户名和密码
5. 执行并复制返回的 access_token

**方式二: 使用 curl**
```bash
curl -X POST http://localhost:8000/api/v1/auth/login \
  -H "Content-Type: application/json" \
  -d '{"username":"admin","password":"admin123"}'
```

#### 3. 设置 Token
在浏览器控制台 (F12) 执行:
```javascript
localStorage.setItem('access_token', '你的token');
```

#### 4. 刷新页面查看 Dashboard
刷新页面后，Dashboard 将自动加载真实数据！

---

### 📊 数据统计

**维度数据**
- 测试用户: 2 个
- 部门: 6 个
- 员工: 23 名
- 仓库: 3 个
- 往来单位: 30 个
- 商品: 30 个

**业务数据**
- 销售订单: 596 个 (过去 90 天)
- 订单明细: 2078 条
- 库存记录: 49 条 (10 条低于预警线)
- 财务流水: 150 条

**AI 分析视图**
- ✓ view_bi_sales_analysis (2078 条)
- ✓ view_bi_inventory_alert (49 条)
- ✓ view_bi_finance_monitor (150 条)
- ✓ view_bi_purchase_analysis

---

### 🛠️ 管理命令

**停止服务**
- 后端: 在终端按 `Ctrl+C`
- 前端: 在终端按 `Ctrl+C`

**重新初始化数据库**
```bash
cd backend
python3 -m scripts.init_db
```

**查看数据库**
```bash
psql -U postgres -d inventory_bi
```

---

### 📍 API 端点

**认证接口**
- POST `/api/v1/auth/login` - 登录
- GET `/api/v1/auth/me` - 获取当前用户信息
- POST `/api/v1/auth/register` - 注册用户

**Dashboard 接口**
- GET `/api/v1/dashboard/overview` - 获取完整仪表盘数据
- GET `/api/v1/dashboard/kpi` - 获取 KPI 数据

**业务接口**
- POST `/api/v1/business/products` - 创建商品
- POST `/api/v1/business/inbound` - 采购入库
- POST `/api/v1/business/outbound` - 销售出库

---

### 🎯 测试建议

1. **登录测试**: 使用 admin/admin123 登录
2. **Dashboard 测试**: 查看 KPI、趋势图、库存预警
3. **业务操作**: 测试入库、出库功能
4. **API 测试**: 使用 Swagger UI 测试所有接口

---

### 💡 提示

- 前端会自动从 localStorage 读取 Token
- 如果 401 错误，重新登录获取新 Token
- 数据库数据可随时重新初始化
- API 文档提供完整的接口测试功能

---

🎉 开始使用进销存 BI 系统吧！
